# Third-Party Notices

Open BIM Information Platform uses third-party open source software.

This file is generated from the dependency metadata of:

- Backend: `pip-licenses --format=json`
- Frontend: `license-checker --json`

## 再生成方法

```bash
./scripts/sbom.sh
```

上記スクリプトは `SBOM.cyclonedx.json` を生成します。リリース時は最新のSBOMを添付し、
本ファイルに依存パッケージのライセンス一覧を反映してください。

> 注: 本ファイルはドラフトです。リリース前に依存パッケージのライセンス一覧を
> 生成物から展開し、法務レビューを受けてください。
